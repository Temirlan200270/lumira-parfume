# Журнал правок

Файл остался от прототипа Essence (опечатки в нотах, `console.log` в корзине, квиз). **Это не текущий backlog.**

Актуальные инварианты смотрите в тестах:

- `src/lib/inventory.test.ts` — состав и запрет снятых брендов/SKU
- `src/lib/catalog-filter.test.ts` — пол, формат, цена за 5 мл, бренд
- `src/lib/search.test.ts` — поиск
- `src/lib/order.test.ts` — цены, телефон, согласие

Историю изменений — в git.
